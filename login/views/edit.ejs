<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>App</title>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10 ">
              <div class="flex justify-end w-full">
        <a class="bg-red-400 rounded-md px-3 py-2 text-sm mb-5 inline-block hover:bg-red-600 curser-pointer" href="/logout">logout</a>
        </div>

            <h2 class="mb-5">edit your.</h2>
            <form action="/update/<%= post._id %>" method="post">
                <textarea class="resize-none border-2 outline-none border-zinc-500 rounded-md w-1/3" placeholder="what's on your mind" name="content"> <%= post.content %> </textarea>
                <button class="px-3 py-2 bg-yellow-500 hover:bg-yellow-900 text-black rounded-md curser-pointer block text-sm mt-2 w-40">Update Post</button>
            </form>




        </form>
    </div>
</body>
</html>