<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>App</title>
</head>
<body>
    <div class="h-screen w-full bg-zinc-900 text-white p-10">
        <form class="flex flex-col w-80 gap-5 ml-[550px] border-2 p-10 rounded-xl" action="/create" method="post">
            <h2 class="text-3xl mb-5">Create User</h2>
            <input 
            class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-500"
            type="text"
            placeholder="Username"
            name="username"
            >

             <input 
            class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-500"
            type="email"
            placeholder="email"
            name="email"
            >


             <input 
            type="password"
            class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-500"
            placeholder="password"
            name="password"
            >


             <input 
            type="number"
            class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-500"
            placeholder="age"
            name="age"
            >

            <button class="px-5 py-2 bg-blue-500 hover:bg-blue-900 rounded-xl curser-pointer">Create User</button>
        </form>
    </div>
</body>
</html>